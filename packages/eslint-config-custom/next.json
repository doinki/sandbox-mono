{
  "$schema": "https://json.schemastore.org/eslintrc",
  "extends": ["sandbox/next", "sandbox/tailwindcss", "./react.json"],
  "rules": {
    "import/no-extraneous-dependencies": [
      "error",
      { "devDependencies": ["apps/*/next.config.js"] }
    ]
  },
  "settings": { "next": { "rootDir": ["apps/*"] } }
}
